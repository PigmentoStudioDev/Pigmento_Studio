{
  "$comment": "Reglas de estructura: no miran valores, miran que cada pieza este donde el sistema asume que esta. Las dos existen porque su fallo ya ocurrio y ninguno produjo error de build.",
  "sassConfigPartial": {
    "why": "cada .scss que Next compila es una unidad de compilacion independiente: la configuracion de index.scss no alcanza a los demas entries. Un entry sin este partial carga Carbon con sus defaults y emite el literal 'IBM Plex Sans' que next/font no registra. Paso en preview.scss: los 4 estilos serif y los 2 de code cayeron al fallback del sistema y nadie se entero",
    "scanDir": "src",
    "partial": "carbon-config",
    "exemptFiles": {
      "src/app/(payload)/custom.scss": "La hoja del PANEL de Payload, no del sitio. Cargar carbon-config aqui meteria los tokens de marca en una UI que no es nuestra, y el fallo del que este gate protege —que next/font no registre 'IBM Plex Sans'— no le aplica: el admin no usa nuestras fuentes. Se queda vacia y presente porque es el punto de extension que Payload genera; borrarla obliga a editar el layout generado y a pelearse con cada regenerado."
    }
  },
  "globalClasses": {
    "why": "preview/preview.scss solo se carga en /ds. Una clase usada fuera de preview/ tiene que estar definida en la hoja global o renderiza sin estilo, sin error. Paso con .pg-landing, que vivia en preview.scss y la usaba la home",
    "prefix": "pg-",
    "globalSheet": "src/design-system/styles/_app.scss",
    "scopedDir": "src/design-system/preview"
  }
}
