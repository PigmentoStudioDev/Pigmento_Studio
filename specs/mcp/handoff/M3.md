# M3 — Conexión desde Claude Code, docs y handoff
Fecha: 2026-09-20 · Agentes: sesión principal + `code-reviewer` + `security-reviewer`

## DoD
- [x] `.mcp.json` en la raíz; Claude Code lo ve desde el worktree:
      ```
      pigmento-cms:
        Scope: Project config (shared via .mcp.json)
        Status: ⏸ Pending approval (run `claude` to approve)
        Type: http · URL: http://localhost:3000/api/mcp
        Headers: Authorization: Bearer ${PIGMENTO_MCP_KEY}
      ```
- [ ] **La sesión interactiva desde Claude Code queda para Karen**: un servidor de proyecto
      pide aprobación al abrir `claude`, y esta sesión no puede aprobarse a sí misma ni cargar
      un MCP nuevo sin reiniciar. Lo que sí atravesó el mismo cable (JSON-RPC sobre
      `/api/mcp` con bearer) es el oráculo de M2: lectura de `pigmento://cms/guia`,
      `findProjects`, subida a R2 con alt en dos idiomas y borrado. Receta para Karen abajo.
- [x] Descripciones de colección revisadas con lo que costó en las pruebas: que `cover` es un
      id de `media`, que el token de una propuesta se ignora, que `alt` se localiza.
- [x] `./scripts/gates.sh` verde — salida al final de este archivo. Y por el cable, contra
      `pnpm dev`, una subida REAL desde `upload.wikimedia.org` (DNS de verdad) que llegó a R2 y
      se borró; `https://localtest.me/x.png` (resuelve a 127.0.0.1) → `invalid_url`.
- [x] PR abierto contra `main`

## Archivos tocados
`.mcp.json`, `.env.example` (`PIGMENTO_MCP_KEY`), `CLAUDE.md` (sección "El MCP del CMS"),
`CHANGELOG.md`, `specs/mcp/README.md` y estos handoffs.

## Receta para Karen (5 minutos)
1. `pnpm dev` en el worktree (o en `main` cuando el PR entre) y `/admin` → **MCP → API Keys**
   → crear una key para tu usuario, todos los toggles encendidos. Copiar la key.
2. `export PIGMENTO_MCP_KEY=<key>` y abrir `claude` en el repo: aprobar `pigmento-cms`.
3. Pedirle: "lee pigmento://cms/guia, sube esta imagen como cover y crea un proyecto en
   borrador con ella". El proyecto debe aparecer en `/admin` como draft.
4. Opcional: `MCP_API_KEY=<key> node scripts/mcp-oraculo.mjs`.

## Lo que se devuelve a Karen
- La key de la base local (`2f8af323-…`) se acuñó por script para las pruebas: bórrala o
  úsala, es solo local. En producción las keys se crean desde el panel.
- Producción: el MCP viaja con S7 (deploy bloqueado). Cuando exista, `.mcp.json` apunta a
  localhost a propósito; para la URL pública basta un `claude mcp add` con esa URL.
- Sin resolver en este programa (D9): prompts MCP con recetas editoriales, globals de S6.

## Gates
```
== Gate 1 — build
  ok  pnpm build compila (y deja .next/ para medir budgets)
== Gate 2 — estatico
  ok  sin secretos hardcodeados
  ok  eslint limpio (hooks y core-web-vitals)
== Gate 3 — contrato
contrato completo en verde · 4 notas (las mismas cuatro de main: archivos >400 lineas)
  ok  contrato completo en verde
== Gate 4 — payload
  ok  artefactos de Payload al dia
== Gate 5 — tests
  ok  vitest verde — Tests  955 passed (955)   ← tras la segunda vuelta de seguridad, con Next 16.3.5
0 fallos, 0 avisos
```
