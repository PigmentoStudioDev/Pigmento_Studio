# M0 — Aprobación e instalación
Fecha: 2026-09-20 · Agentes: sesión principal

## DoD
- [x] Aprobación de Karen — "puedes correr comandos e instalar paquetes. tienes mi permiso"
- [x] `cp ../../Pigmento_Studio/.env .env` y `cp ../../Pigmento_Studio/pigmento.db .` — misma
      base y mismas credenciales de R2 que el checkout principal (los dos ignorados por git)
- [x] `pnpm install` — `Done in 7.3s`; sharp compilado (`sharp ok 0.35.4`)
- [x] `pnpm add @payloadcms/plugin-mcp@3.88.0` → `"@payloadcms/plugin-mcp": "3.88.0"`
- [x] `pnpm add zod@3.25.76` — no estaba en la spec. El plugin y el SDK de MCP piden
      `zod ^3.25`; las tools propias declaran sus parámetros con el mismo `z` que el
      plugin, así que tiene que ser la misma major. Primero entró la 4.6.5 (la que resuelve
      `pnpm add zod` a secas) y se bajó a la que el plugin ya había resuelto.
- [x] `pnpm test` con el plugin instalado y sin usar: 925 tests verdes (la spec decía 559:
      la cifra era de S5, el repo creció desde entonces)

## Archivos tocados
`package.json`, `pnpm-lock.yaml`.

## Lo que se devuelve a Karen
- `pnpm` avisa `Unsupported engine: wanted node 24.x (current v22.23.2)`. Todo corrió igual;
  no se tocó.
