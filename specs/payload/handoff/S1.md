# S1 — El budget de CSS, antes de que Payload exista
Fecha: 2026-09-09 · Agentes: conformance-smith -> code-reviewer

## DoD
- [x] `pnpm build && pnpm conformance budgets` verde, CSS **igual** a 161kb raw / 18kb gzip (limite 200/20)
- [x] Test nuevo verde y **verificado en rojo** — evidencia abajo
- [x] `./scripts/gates.sh` verde — 0 fallos, 0 avisos (515 tests)

## Archivos tocados
- `scripts/conformance.mjs` — `firstLoadFiles()` lee CSS y JS del HTML publico; salta rutas que contienen `(payload)`; sin HTML publico sigue devolviendo `null` y quien llama cae al total
- `conformance/budgets.json` — `$why` del artefacto `css` (A3). Limites 200/20 intactos. JS no se toco
- `src/design-system/styles/__tests__/budget-scope.test.ts` — nuevo, en la lista de S1

Cero git. Cero instalaciones. Cero cambios a `package.json` / `.npmrc` / `tsconfig.json`.

## firstLoadFiles
Lee `<link rel="stylesheet" href="/_next/static/...css">` y los `.js` de `/_next/static/` en el HTML de `.next/server/app`. Ignora cualquier HTML cuya ruta contenga `(payload)`. Si no hay HTML publico, `null`.

El HTML real de este Next 16.3.2 (orquestador): `rel="stylesheet"` antes de `href`, archivos en `static/chunks/*.css`. El regex pega. `payload_html = 0` (Payload no esta instalado).

## DoD 1 — cifras
ANTES (runner viejo, S0 / 2026-09-09): 161kb raw / 18kb gzip
DESPUES (orquestador):

```
conformance budgets

— budgets
  OK    [budgets] css: 161kb raw / 18kb gzip (limite 200/20)
  OK    [budgets] js: 693kb raw / 216kb gzip (limite 780/230)

contrato completo en verde
```

Comparado con 161/18: **igual**. Hoy todo el CSS emitido ya lo piden paginas publicas (incluido `/ds`). El recorte aparece cuando hay un `.css` que ningun HTML publico referencia.

## DoD 2 — rojo y verde

**Rojo 1 (smith): css falso ~104kb + `firstLoadFiles` revertido a `if (ext !== '.js') return null`**

```
conformance budgets

— budgets
  FAIL  [budgets] css: 263kb raw > 200kb
  OK    [budgets] js: 693kb raw / 216kb gzip (limite 780/230)

1 violaciones de contrato
```

**Verde (mismo css falso, runner nuevo — la cifra no sube)**

```
conformance budgets

— budgets
  OK    [budgets] css: 161kb raw / 18kb gzip (limite 200/20)
  OK    [budgets] js: 693kb raw / 216kb gzip (limite 780/230)

contrato completo en verde
```

**Rojo 2 (orquestador, independiente, sin revertir codigo): `.next/static/css/payload-admin.fake.css` 106507 bytes**

```
-- runner NUEVO con falso --
  OK    [budgets] css: 161kb raw / 18kb gzip (limite 200/20)

-- walk viejo (todo .css bajo .next/static) --
OLD_WALK files 5 rawKb 265
```

El falso no queda en el arbol. Test:

```
 ✓ src/design-system/styles/__tests__/budget-scope.test.ts (1 test) 87ms
 Test Files  1 passed (1)
```

Rojo reproducido: **si** (smith con el bug puesto; orquestador con walk vs runner nuevo).

## Gates
`./scripts/gates.sh`: **verde**. 0 fallos, 0 avisos.

```
== Gate 1 — build
  ok  pnpm build compila (y deja .next/ para medir budgets)

== Gate 2 — estatico
  ok  sin secretos hardcodeados
  ok  eslint limpio (hooks y core-web-vitals)

== Gate 3 — contrato
  OK    [style] 40 archivos
  OK    [tsx] 72 archivos
  OK    [react] 107 archivos
  OK    [structure] 32 entries de Sass
  nota  [modularity] SiteHeader.module.scss: 408 lineas
  nota  [modularity] SiteHeader.tsx: 447 lineas
  OK    [modularity] 146 archivos bajo el tope de 800
  OK    [modularity] 189 imports internos
  OK    [budgets] css: 161kb raw / 18kb gzip (limite 200/20)
  OK    [budgets] js: 693kb raw / 216kb gzip (limite 780/230)
  ok  contrato completo en verde

== Gate 4 — tests
  ok  vitest verde — Tests  515 passed (515

0 fallos, 0 avisos
```

Gates nuevos: `budget-scope` — rojo reproducido: si. C6 `panel-fuera-del-budget` — no se escribio (fuera de la lista de S1).

Primera pasada de gates salio **roja** por el test: import relativo `../../../../scripts/conformance.mjs` salia de `design-system/`. Bug propio del smith. Se reescribio el test para afirmar el runner por `spawnSync` y reproducir el walk viejo con `node:fs`. Segunda pasada: verde. code-reviewer posterior: 0 criticos, 0 altos.

## C6 panel-fuera-del-budget
Estado: **deuda hasta S2**.
Motivo: Payload no esta instalado. El rojo de C6 es «devolver `firstLoadFiles` a barrer `.next/server/app` entero, con Payload instalado». Sin panel no hay HTML que intersectar. No se creo `scripts/payload-build-check.mjs` ni se toco `gates.sh` (fuera de la lista de S1). `firstLoadFiles` ya salta `(payload)` por ruta de archivo.

## Decisiones tomadas dentro del alcance
1. Limites de CSS no se tocaron (200/20).
2. El test no importa `scripts/` desde el design system: el gate de modularidad lo rechaza. El contrato se afirma por la salida del CLI.
3. C6 como checker independiente espera a S2.

## Lo que se devuelve a Karen
Nada que bloquee. Anotaciones para S2 (sigue bloqueada por aprobacion de deps):

1. El skip `page.includes('(payload)')` mira la ruta del HTML. En este Next 16 los HTML se nombran por URL (`es.html`, `en/ds.html`), no por el route group. Si el panel prerenderiza a `.next/server/app/admin.html`, el skip no pega y el CSS del admin entra al first-load. Margen: 2kb gzip. C6 tiene que verificarse en rojo en S2 con el HTML real del panel.
2. Si el regex de CSS no pega (href antes de rel, u otro markup), `firstLoadFiles` devuelve `null` y `sectionBudgets` cae a `walk()` — el bug viejo, en silencio. Hoy el markup tiene rel antes de href. Vigilarlo cuando entre Payload.
3. `/ds` ya cuenta en el first-load. El techo ya lo incluye.

S2 no se arranca en este turno.
